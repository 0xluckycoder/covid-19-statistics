export default function IconComponent(iconsObj) {
    // const totalCasesElement = document.querySelector('.total-cases');
    // const newCasesElement = document.querySelector('.new-cases');
    // const inHospitalElement = document.querySelector('.in-hospital');
    // const deathsElement = document.querySelector('.deaths');
    // const recoveredElement = document.querySelector('.recovered');

    // console.log(totalCasesElement);

    // const initTotalCases = new Image();
    // const initNewCasesElement = new Image();
    // const initInHospitalElement = new Image();
    // const initDeathsElement = new Image();
    // const initRecoveredElement = new Image();

    // initTotalCases.src = iconsObj.totalCases;
    // initNewCasesElement.src = iconsObj.newCases;
    // initInHospitalElement.src = iconsObj.inHospital;
    // initDeathsElement.src = iconsObj.deaths;
    // initRecoveredElement.src = iconsObj.recover;

    // totalCasesElement.appendChild(initTotalCases);
    // newCasesElement.appendChild(initNewCasesElement);
    // inHospitalElement.appendChild(initInHospitalElement);
    // deathsElement.appendChild(initDeathsElement);
    // recoveredElement.appendChild(initRecoveredElement);

}